using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Objeto_inter : MonoBehaviour
{
    // Start is called before the first frame update
    public void ActivarObjeto()
    {
        Destroy(gameObject);
    }
}
